function buildPVmat()

% load the salient points with descriptors for 16 images
% maybe, load only the matches between two images (also 16) Make them in
% advance
if nargin<1
    
end

% the Point-view matrix, every row represents an image and every column a
% point
PVmat=[]

% for every point pairs
for pair=1:16
end
    